{
  "name": "hidr4lisk-tech",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build":     "npm run build:css && npm run build:js",
    "build:css": "tailwindcss -i src/styles/main.css -o dist/styles.css --minify",
    "build:js":  "esbuild src/modules/digest/index.js --bundle --minify --format=esm --outdir=dist/modules/digest && esbuild src/modules/news/index.js --bundle --minify --format=esm --outdir=dist/modules/news && esbuild src/modules/changelog/index.js --bundle --minify --format=esm --outdir=dist/modules/changelog && esbuild src/modules/compare/index.js --bundle --minify --format=esm --outdir=dist/modules/compare",
    "dev:css":   "tailwindcss -i src/styles/main.css -o dist/styles.css --watch",
    "dev:js":    "esbuild src/modules/news/index.js --bundle --format=esm --sourcemap --outdir=dist/modules/news --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "tailwindcss": "^3.4.0"
  }
}
